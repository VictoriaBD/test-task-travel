Для сборки проекта должен быть установлен Node.js и выполнены следующие шаги:

* Инициализация проекта и установка необходимых пакетов:

```bash
npm i
```

* Запуск сборки проекта:

```bash
gulp build
```

Финальный макет будет в папке «dist».